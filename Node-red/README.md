模拟节拍流是模拟了设备节拍数据而产生的，主要逻辑是:通过模拟设备最后一个工序的上升沿信号触发时记录下当前时间并且储存，

下一个工件继续流到工序后再次触发上升沿信号触发时间记录，当两个信号时间相减就得出了节拍数据，但是现在是有缺陷的:

1.当设备停机或者工厂下班时上一次时间记录还储存着，等下一次开机时可能隔了十几个小时，然而现在是没有设计去处理这种场景，直接算出节拍相差十几个小时就会误导整个节拍分析系统

URL数据映射流主要的作用是:把PGSQL存储的结果通过webapi映射到了URL地址上供HTML做数据源
